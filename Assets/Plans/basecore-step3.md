# BaseCore HP Fix - Method Edits

1. Update fields in `BaseCore.cs`.
2. Update `CacheWorldHpComponents` in `BaseCore.cs`.
3. Update `RefreshWorldHpBar` in `BaseCore.cs`.
4. Update `LateUpdate` in `BaseCore.cs`.
